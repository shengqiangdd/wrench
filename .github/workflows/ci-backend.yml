name: CI Backend
on:
  push:
    paths:
      - 'backend/**'
      - '.github/workflows/ci-backend.yml'
  pull_request:
    paths:
      - 'backend/**'
      - '.github/workflows/ci-backend.yml'

jobs:
  rust-backend:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: backend
          cache-targets: false
          shared-key: clippy-v2
      - run: cargo check
        working-directory: backend
      - run: cargo clippy -- -D warnings -A clippy::needless_update -A clippy::field_reassign_with_default
        working-directory: backend
      - run: cargo test
        working-directory: backend
